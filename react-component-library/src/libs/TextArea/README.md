```
Renders a <textarea> element that uses a callback function to pass 
its value to the parent component.

Use object destructuring to set defaults for certain attributes of the 
<textarea> element. Render a <textarea> element with the appropriate attributes and 
use the callback function in the onChange event to pass the value of the textarea to the parent.
```